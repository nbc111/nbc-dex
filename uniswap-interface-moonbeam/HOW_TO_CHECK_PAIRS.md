# 如何在链上查询交易对

## 📋 概述

要让交易对在链上可查，需要先**创建流动性池**。目前您只是在前端配置了代币，但交易对（Pair 合约）还不存在于链上。

## 🔍 查询方法

### 方法 1：使用提供的脚本查询

#### 步骤 1：安装依赖
```bash
npm install ethers
```

#### 步骤 2：配置 RPC URL
编辑 `scripts/check-pairs.js`，将第 9 行的 `YOUR_NBC_RPC_URL` 替换为您的 NBC 链 RPC 地址：

```javascript
const NBC_RPC_URL = 'http://your-nbc-rpc-url:port'; // 例如: http://localhost:8545
```

#### 步骤 3：运行查询脚本
```bash
node scripts/check-pairs.js
```

**输出示例：**
```
🔍 开始查询 NBC 链上的交易对...

Factory 地址: 0xf0616CCDa274b6DbFa645d70f8Dc0f617707E793
RPC URL: http://localhost:8545

✅ 已连接到网络，ChainId: 1281

============================================================
检查 NBC 交易对
============================================================

✅ NBC/ETH 交易对已存在
   Pair 地址: 0x1234...
   Reserve0: 1000.0
   Reserve1: 2.5
   LP Token 总供应量: 50.0

❌ NBC/SOL 交易对不存在
   需要先创建流动性池

============================================================
所有交易对概览
============================================================

📊 Factory 中共有 1 个交易对

所有交易对列表：
1. NBC/ETH - 0x1234...

✅ 查询完成！
```

---

### 方法 2：使用区块链浏览器查询

如果您的 NBC 链有区块链浏览器（类似 Etherscan），可以：

1. **访问 Factory 合约**
   - 地址: `0xf0616CCDa274b6DbFa645d70f8Dc0f617707E793`

2. **调用 `getPair` 函数**
   - 参数 1 (tokenA): NBC 地址 `0x90b23532950f99cdcdcadeaf5f02435419e689e31ef3f716f04a6c5b1dfec9fa`
   - 参数 2 (tokenB): 例如 ETH 地址 `0x934EbeB6D7D3821B604A5D10F80619d5bcBe49C3`
   - 如果返回 `0x0000...0000`，说明交易对不存在
   - 如果返回其他地址，那就是 Pair 合约地址

3. **查看 Pair 合约详情**
   - 访问返回的 Pair 地址
   - 查看储备量（reserves）
   - 查看流动性提供者（LP token holders）

---

### 方法 3：使用 Web3 控制台

在浏览器控制台或 Node.js 中：

```javascript
const { ethers } = require('ethers');

const provider = new ethers.providers.JsonRpcProvider('YOUR_RPC_URL');
const factoryAddress = '0xf0616CCDa274b6DbFa645d70f8Dc0f617707E793';
const factoryABI = ['function getPair(address,address) view returns (address)'];
const factory = new ethers.Contract(factoryAddress, factoryABI, provider);

// 查询 NBC/ETH 交易对
const nbcAddress = '0x90b23532950f99cdcdcadeaf5f02435419e689e31ef3f716f04a6c5b1dfec9fa';
const ethAddress = '0x934EbeB6D7D3821B604A5D10F80619d5bcBe49C3';

const pairAddress = await factory.getPair(nbcAddress, ethAddress);
console.log('NBC/ETH Pair 地址:', pairAddress);
```

---

## 🚀 如何创建交易对

如果查询结果显示交易对不存在，您需要先创建流动性池：

### 选项 A：通过前端界面创建

1. **启动前端应用**
   ```bash
   npm start
   ```

2. **连接钱包**
   - 确保钱包连接到 NBC 链（ChainId: 1281）
   - 确保钱包有足够的代币余额

3. **添加流动性**
   - 访问 "Pool" 页面
   - 点击 "Add Liquidity"
   - 选择交易对（例如 NBC/ETH）
   - 输入金额
   - 确认交易

4. **等待确认**
   - 第一次添加流动性时，会自动创建 Pair 合约
   - 交易确认后，交易对就会出现在链上

### 选项 B：使用智能合约直接创建

```javascript
// 1. 批准代币
await nbcToken.approve(routerAddress, amount);
await ethToken.approve(routerAddress, amount);

// 2. 添加流动性（会自动创建 Pair）
await router.addLiquidity(
  nbcAddress,
  ethAddress,
  nbcAmount,
  ethAmount,
  minNbcAmount,
  minEthAmount,
  yourAddress,
  deadline
);
```

---

## 📊 您的交易对配置

| 交易对 | Token A (NBC) | Token B | Status |
|--------|---------------|---------|--------|
| NBC/ETH | `0x90b23532...` | `0x934EbeB6...` | ⏳ 待创建 |
| NBC/SOL | `0x90b23532...` | `0xd5eeccc8...` | ⏳ 待创建 |
| NBC/BNB | `0x90b23532...` | `0x9c432374...` | ⏳ 待创建 |
| NBC/XRP | `0x90b23532...` | `0x48e17725...` | ⏳ 待创建 |

**Factory 地址:** `0xf0616CCDa274b6DbFa645d70f8Dc0f617707E793`  
**Router 地址:** `0x3d53f590c82a61f85e6B1f0813e509AEAA0b4991`

---

## ⚠️ 重要提示

1. **交易对创建是永久的**
   - 一旦创建，Pair 合约地址不会改变
   - 即使流动性为 0，Pair 合约仍然存在

2. **初始流动性很重要**
   - 第一次添加流动性会设定初始价格
   - 建议根据市场价格合理设置比例

3. **需要足够的 Gas**
   - 创建 Pair 需要较多 Gas（部署新合约）
   - 确保钱包有足够的原生代币支付 Gas

4. **代币授权**
   - 添加流动性前需要先授权 Router 合约
   - 每个代币只需授权一次

---

## 🔗 相关资源

- **Uniswap V2 文档**: https://docs.uniswap.org/protocol/V2/introduction
- **Factory 合约**: `0xf0616CCDa274b6DbFa645d70f8Dc0f617707E793`
- **Router 合约**: `0x3d53f590c82a61f85e6B1f0813e509AEAA0b4991`

---

## 📞 需要帮助？

如果您需要：
- ✅ 批量创建流动性池的脚本
- ✅ 自动化部署工具
- ✅ 流动性管理策略

请随时告诉我！
